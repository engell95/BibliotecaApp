const primaryColor = "#008866";
const errorColor= "#D43C37";
const infoColor = "#00ABEC";
const processingColor = "#33BA75";
const successColor = "#00583C";
const warningColor= "#F4AD3D";
const textPrimaryColor = "#FFFFFF";

const HeaderCSS = {
    "background-color":primaryColor,
    background:primaryColor,
    color: textPrimaryColor,  
} 

const SiderCSS = {
    "background-color":primaryColor,
    background:primaryColor,
    color: textPrimaryColor,
} 

const FooterCss = {
    "background-color":primaryColor,
    background:primaryColor,
    color: textPrimaryColor,
}

const LoginCss = {
    "background-color":primaryColor,
    background:primaryColor,
    color: textPrimaryColor,
}

const LogoutCss = {
    "background-color":primaryColor,
    background:primaryColor,
    color: textPrimaryColor,
}

export { HeaderCSS, SiderCSS,FooterCss,LoginCss,LogoutCss };